+++
title = 'Undoing a Commit'
time =45
[objectives]
    1="Use `git revert` to undo a commit"
    2="Reset the repository to a previous version with `git reset`"
[build]
  render = 'never'
  list = 'local'
  publishResources = false

+++

TODO:

- Add some extra content to an existing repo (education blog?)
- Revert a commit and examine history
- Reset to a previous commit and examine history

