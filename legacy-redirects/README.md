# legacy-redirects

These are configs for domains we used to use for hosting bits of the curriculum.

There is a directory per former domain, with redirect config specific to that domain contained therein.
